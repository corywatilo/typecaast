/**
 * `@typecaast/core/mocks` — hand-authored `SimState` fixtures + a faked
 * playback stub for UI-first development (M0.10). No real engine; swapped for
 * the real `compile`/`getStateAt`/player in M1-engine behind the same contract.
 */

export * from "./billing-toast.js";
export * from "./mock-player.js";
