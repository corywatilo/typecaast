---
"@typecaast/builder": patch
---

Add an optional `logo` prop to `<Builder>`, rendered immediately left of the header wordmark (mirroring `headerNav`/`headerActions`). Lets a host inject its brand mark into the builder/playground header.
